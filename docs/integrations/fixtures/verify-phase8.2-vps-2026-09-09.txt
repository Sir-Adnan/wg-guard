Phase 8.2 — secure access and persistent-manager acceptance
Date: 2026-09-09; dedicated Ubuntu 24.04.4 LTS amd64 VPS
Final candidate: 13a549293962cc172e9dde53fff8f6da3ce81ff8

PASS: exact-commit GitHub source update built the Docker runtime and recovered the
schema-three empty-exposure migration sentinel left by the preceding candidate. The
live boot configuration was reconstructed as direct built-in ACME without claiming
certificate readiness; `tls-check` then proved the trusted domain certificate and
persisted `verified`. The container remained healthy.

PASS: post-install direct -> private -> managed-Nginx/webroot transitions were
transactional. Private mode bound only 127.0.0.1. Managed Nginx retained public TCP
80/443 and proxied to 127.0.0.1:8080. A production Let's Encrypt domain certificate
was issued (SAN zedutch.zenmode.ir, expiry 2026-12-08), the managed key was 0600,
the deploy hook was 0700, the renewal timer was enabled/active, and every access
check in `doctor` passed. HTTPS returned one canonical HSTS header.

PASS: while Nginx owned 80/443, an explicit direct built-in request failed before
mutation; both the container identity and install-state digest remained unchanged.
The ordinary renewal command correctly skipped a certificate that was not due. A
Certbot staging dry-run with deploy hooks completed successfully and the service
remained healthy, without successful-hook output being mislabeled as an error.

PASS: Let's Encrypt staging and production public-IP issuance both succeeded with
the mandatory short-lived profile (critical SAN IP Address:178.105.13.15; production
expiry 2026-09-15). Direct HTTPS on 443 was browser-trusted by the host, returned one
HSTS header, and recorded the deterministic lineage. Renewal dry-run invoked the
locked Docker reload hook and the recreated container returned healthy.

PASS: the installed `sudo wg-guard` manager opened locally at a real 40-column SSH
width with the state-aware English menu. `exposure configure --help` displayed its
options and exited successfully. Existing Phase 8.1 zero-network bootstrap, native,
Telegram, QR/config/client and lifecycle evidence was not repeated because those
paths were unaffected.

Automated only: Cloudflare DNS-01 secret transport/plugin commands, Origin CA,
manual certificate, external proxy, Native runtime rendering, rollback/failure
injection, state/path validation and 40/48/80-column terminal cases. No scoped
Cloudflare test token was available, so no real DNS mutation or DNS-01 issuance is
claimed. Native secure-exposure recertification remains part of the Phase 11 matrix.

PASS cleanup: the original immutable phase7v4 Docker deployment and host binary were
restored and healthy. Test Nginx packages, Certbot certificates/snaps/snapshot,
managed hook/webroot, uploaded binaries and private test snapshots were removed.
No public tag, release, registry image or final artifact was published. No token,
private key, certificate body, password or raw WG-Guard configuration is recorded
in this evidence.

Corrective installer acceptance (same date; candidate 5d622275b0d57c39f6af828b87e09670ec1a3d07):

PASS: an exact-commit GitHub one-line source acquisition on Ubuntu 24.04.4 amd64
emitted bounded 15-second progress, persisted the manager, and completed Docker
installation with the recommended source-backed `awg-2026-09` bundle. The host
module came from kernel tag v3.1.20260906 / commit 4569c4c67f3a57414969260cafbbd04694fbaae0;
the container tool came from v3.1.20260812 / commit ee0f0a9aa34ff0a0da4b3433b9512781cfe02843.
No Amnezia PPA was present. DKMS, image labels, binary revision and HTTP health all
matched the selected identities.

PASS: real unattended-upgrades lock contention stopped the first package attempt
without state/data mutation. The correction uses the official APT lock wait; the
next exact retry carried observed package ownership through the safe aborted state,
repaired the inactive/stale Docker systemd socket, and completed. Re-running the one-line entry at
40 columns used the cached manager with no acquisition. Explicit data/package purge
removed state, data, binary, source cache, managed AWG tool and versioned DKMS source;
Docker service/socket were inactive and the uninstall journal was complete.

Unchanged Phase 8/8.1 native, Telegram, QR/config/client, backup/restore and two-way
update/rollback evidence was reused rather than repeated. No API/OpenAPI contract or
public release was changed.

Update-aware manager maintenance acceptance (same date; final code candidate
63194d0ac037ad6dd40fad23d45014058de5d4b2):

PASS: a fresh exact-commit Docker install from candidate b69ff859c3fd completed with
the source-backed awg-2026-09 bundle, private panel health and separate root-owned
manager/receipt paths (0700 directory, 0755 binary, 0600 receipt). Re-running the
same GitHub entry performed the identity check, reported the manager current and did
not rebuild. `update all` created one lifecycle backup, built/deployed a new immutable
Docker image, returned HTTP health, synchronized host/container versions and closed
the core journal complete with module identity matches-disk.

PASS: the GitHub entry then advanced only the manager from b69ff859c3fd to
9045ead43048; the installed panel version and service-binary SHA-256 remained
unchanged. From that independently cached manager, `update manager --commit
63194d0ac037ad6dd40fad23d45014058de5d4b2` emitted 15-second heartbeats, promoted
the new manager, and a repeat reported current without acquisition. Update status
honestly showed manager 63194d0ac037, panel b69ff859c3fd and core awg-2026-09
matches-disk. The real nested-menu cancel returned to management without false
success or a technical cancellation error.

Automated fixtures additionally changed simulated `main` twice and proved exactly
one source build, metadata-only subsequent checks, strict invalid-selection and
`--refresh` refusal, and verified-cache fallback on transport failure. No stable
release existed, so published-release update was not claimed. No alternate core
bundle exists beyond the recognized legacy entry; a real cross-bundle core transition
was not claimed. Native, Telegram, TLS, QR/config/client and unrelated lifecycle
drills were not repeated. Final purge removed the test container, state, data, host
binary and manager/core cache.

Administrator/recovery maintenance acceptance (2026-09-10; final candidate
29d26220d8c9eec66215c15f6ece2df656846880):

PASS: a clean Ubuntu 24.04.4 amd64 host acquired the exact GitHub commit and
completed the recommended source-backed Docker installation. An intentionally
short password stayed in the administrator prompt and displayed the bounded retry
message; no failed owner attempt or recovery journal was created. The explicit
username prompt selected `admin` on Enter, and the shared database contained one
enabled `admin` account with the internal protected `owner` role. No password or
hash is recorded here.

PASS: Docker used its configured `/var/lib/docker` image store rather than an
application copy under `/opt`. The generated Compose file was
`/etc/wg-guard/compose.yaml`; read-only config and persistent data bind mounts were
`/etc/wg-guard/wg-guard.toml` and `/var/lib/wg-guard` respectively.

The first candidate exposed AUD-049: credentials were accepted, but private
loopback HTTP had been labelled proxy TLS, producing a Secure session cookie that
could not return over the documented SSH tunnel. The final candidate resolved
private exposure to loopback-only dev transport. Its container was running and
healthy; an HTTP login using secret stdin finished at `/` with status 200 and one
session cookie. Real Nginx/external-proxy behavior was not repeated because those
paths remain proxy mode and their earlier HTTPS acceptance evidence is unchanged.

PASS: **Remove everything** stopped the container, removed the state-recorded
deployment, data, keys/backups, reviewed AWG source/DKMS ownership, installer-owned
Docker package, `/etc/wg-guard`, `/var/lib/wg-guard`, `/var/cache/wg-guard`,
`/var/log/wg-guard`, `/usr/local/bin/wg-guard`, and the module-load entry. The
manager exited cleanly and the final stage label contained no internal catalog key.
APT-managed shared dependencies were not autoremove-purged. The generated-password
show-once path remains automated lifecycle-tested rather than captured in this
artifact, so no credential is disclosed. API/OpenAPI was unchanged and Phase 9 was
not started.
